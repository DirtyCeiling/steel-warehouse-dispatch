# 大冶特钢钢材库智能调度系统 — 软件设计方案

> **文档范围**：库管调度规划 / 机器狗任务规划管理系统 / 数字孪生系统
> **版本**：v1.1
> **日期**：2026-05-08

---

## 目录

- [第一部分：库管调度规划](#第一部分库管调度规划)
- [第二部分：机器狗任务规划管理系统](#第二部分机器狗任务规划管理系统)
- [第三部分：数字孪生系统](#第三部分数字孪生系统)
- [附录：三系统间接口矩阵](#附录三系统间接口矩阵)

---

# 第一部分：库管调度规划

## 1. 系统定位

库管调度规划是智能调度机器狗系统的**核心规划推荐引擎**，定位为**半自动化**——系统生成调度方案和建议，由管理工确认后下发执行。管理工始终保留最终决策权。

**设计原则**：
- 系统做"计算和推荐"，人做"确认和决策"
- 所有调度方案需经管理工确认后才下发至天车工/调运工
- 管理工可在任意环节干预、修改或否决系统方案
- 逐步演进：Phase 1 以辅助决策为主，后续阶段可逐步提升自动化程度

**核心职责**：

- 接收多源数据（半成品库物流系统、ERP/订单系统、门卫扫码系统、机器狗扫描数据）
- 自动生成入库/出库/倒库推荐方案，供管理工审核
- 管理钢材库存状态，维护垛位-钢材映射关系
- 管理工确认后，向天车工/调运工分发任务指令

### 与原管理工职责的映射

| 管理工职责 | 系统角色 | 说明 |
|-----------|---------|------|
| 扫描货车，识别钢材 | **自动执行** | 数据融合引擎 + 机器狗扫描识别，无需人工 |
| 判断放哪个货架 | **推荐 + 确认** | 系统生成推荐垛位，管理工确认或调整 |
| 判断吊哪根钢 | **推荐 + 确认** | 系统计算最优出库源，管理工确认或改选 |
| 处理压货情况 | **推荐 + 确认** | 系统生成倒库方案，管理工确认或调整 |
| 指挥天车工 | **自动分发** | 确认后自动通过大屏/语音/小程序下发 |
| 确认任务完成 | **自动判定 + 人工复核** | 系统比对数量，异常时人工介入 |

## 2. 功能模块架构

```
┌─────────────────────────────────────────────────────────────────┐
│                       库管调度规划                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 入库规划模块  │  │ 出库规划模块  │  │ 倒库规划模块  │              │
│  │             │  │             │  │             │              │
│  │ ·来车预知   │  │ ·提货单接收  │  │ ·压货识别   │              │
│  │ ·扫描复验   │  │ ·提前盘库   │  │ ·路径规划   │              │
│  │ ·垛位推荐   │  │ ·目标定位   │  │ ·临时存放   │              │
│  │ ·归位确认   │  │ ·吊运排序   │  │ ·归位整理   │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────┐                │
│  │           规划推荐引擎（核心）                  │                │
│  │                                             │                │
│  │  ·入库推荐算法    ·出库调度算法               │                │
│  │  ·倒库决策算法    ·天车任务队列分配算法        │                │
│  │  ·方案排序与解释  ·人工确认/调整接口          │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │           人工确认工作台                       │                │
│  │                                             │                │
│  │  ·方案审核界面    ·一键确认/逐项调整           │                │
│  │  ·否决与重新生成  ·确认日志与追溯             │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │              数据融合引擎                     │                │
│  │                                             │                │
│  │  ·多源数据清洗  ·数据比对与补全               │                │
│  │  ·冲突仲裁      ·统一数据视图                 │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │              库存状态管理                     │                │
│  │                                             │                │
│  │  ·垛位-钢材映射  ·库存变更日志               │                │
│  │  ·实时库存快照  ·历史库存回溯                 │                │
│  └─────────────────────────────────────────────┘                │
│                                                                 │
│  ┌─────────────────────────────────────────────┐                │
│  │              外部接口层                       │                │
│  │                                             │                │
│  │  ·半成品库物流系统接口  ·ERP/订单系统接口      │                │
│  │  ·门卫扫码系统接口      ·库管系统API          │                │
│  │  ·天车系统接口（预留）   ·机器狗数据接口        │                │
│  └─────────────────────────────────────────────┘                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 入库规划详细设计

### 3.1 入库业务流程

```
半成品库装车完成
       │
       ↓（场景A：提前30分钟推送）
系统接收：车牌号 + 订单明细（规格×数量×吨数）
       │
       ↓
预生成推荐方案：规格→目标垛位映射（可多方案排序）
       │
       ↓
货车到达 ──→ 机器狗就位于货车尾部
       │
       ↓
机器狗扫描货车断面/内壁（复验）
       │
       ├── 数据一致 → 确认推荐方案
       ├── 数据有差异 → 以扫描数据为准，修正推荐方案
       └── 扫描失败 → 人工介入，标记异常
       │
       ↓
┌─────────────────────────────────┐
│  ★ 人工确认节点                   │
│  管理工在确认界面审核：            │
│  · 查看推荐垛位分配               │
│  · 可调整/否决/逐项确认           │
│  · 确认后方案生效，不可逆          │
└─────────────────────────────────┘
       │
       ↓（确认后）
大屏展示入库任务 / 语音播报指令
       │
       ↓
天车工执行吊运：货车 → 目标垛位
调运工绑扎/拆解
       │
       ↓
机器狗扫描垛位确认：数量变化 = 入库数量
       │
       ↓
库存更新 + 数字孪生同步
任务状态：已完成
```

### 3.2 入库推荐算法

**输入**：
- 待入库钢材列表：`[{规格, 材质, 数量, 吨数, 长度, 订单号}]`
- 当前垛位状态：`[{垛位编号, 区域, 当前规格, 当前数量, 当前层数, 最大层数}]`
- 约束条件：同规格归并优先、空垛位优先、路径最短

**输出**：
- 推荐方案（可多方案排序）：`[{方案ID, 得分, 分配明细: [{钢材ID, 目标垛位编号, 建议层位}], 方案说明}]`
- 推荐天车任务序列：`[{天车编号, 任务序列: [{操作, 钢材ID, 源位置, 目标位置}]}]`

**算法逻辑**：

```
对于每个待入库钢材：
  1. 优先查找"同规格+同材质+有空位"的垛位
     → 若存在，推荐分配到该垛位（同规格归并）
  2. 若无同规格空位，查找"完全空置"的垛位
     → 若存在，推荐分配到该垛位（新规格建立）
  3. 若无空垛位，查找"同规格但接近满载"的垛位
     → 若存在且还有空间，推荐分配到该垛位
  4. 若全部不满足，触发"扩容告警"，通知管理员

多方案生成：
  · 系统生成1-3个可行方案，按"搬运距离+空间利用率+后续出库便利性"综合评分排序
  · 默认选中最高分方案，管理工可切换到备选方案或手动调整

天车分配：
  1. 获取当前各天车任务队列长度
  2. 获取各天车到货车位置的距离
  3. 综合考虑产线-天车映射关系（3条产线↔5个天车）
  4. 推荐任务到最空闲且最近的天车（管理工可调整）

人工确认界面要素：
  · 推荐方案摘要（垛位编号、规格、数量、距离）
  · 该垛位当前状态（已有库存、剩余空间）
  · [确认] [查看备选方案] [手动调整] [否决重新生成]
```

### 3.3 入库状态机

| 状态 | 触发条件 | 系统动作 | 下一状态 |
|------|---------|---------|---------|
| **空闲** | — | 等待来车预通知 | — |
| **预规划** | 半成品库推送来车信息 | 预生成推荐方案（多方案），通知机器狗准备 | 待扫描 |
| **待扫描** | 货车到达 | 机器狗就位，准备扫描 | — |
| **识别中** | 机器狗开始扫描 | 实时回传识别数据，数据融合引擎比对 | — |
| **待确认** | 扫描完成，方案修正完毕 | 推送方案至管理工确认工作台，等待人工审核 | 执行中/预规划 |
| **执行中** | 管理工确认方案 | 大屏展示任务，语音播报指令，天车工开始吊运 | — |
| **完成** | 机器狗复验确认 | 库存落定，数字孪生同步，释放天车资源 | 空闲 |

> **待确认状态说明**：管理工可在确认工作台一键确认、逐项调整或否决。若否决，系统返回预规划状态重新生成方案。确认操作记录到日志，含操作人、时间、调整内容。

## 4. 出库规划详细设计

### 4.1 出库业务流程

```
司机到达 → 提交提货单给门卫
       │
       ↓
门卫扫码录入提货单
       │
       ↓
系统接收订单信息：合同号、规格、型号、数量、吨数
       │
       ↓
系统秒级推送至机器狗
       │
       ↓
机器狗提前到达目标货架，盘库确认：
  · 扫描垛位，确认目标钢材位置
  · 比对系统库存数据
  · 判断是否被压货
       │
       ├── 未被压货 → 生成直接出库推荐方案
       │
       └── 被压货 → 进入倒库决策 → 含倒库步骤的出库推荐方案
       │
       ↓
┌─────────────────────────────────┐
│  ★ 人工确认节点                   │
│  管理工在确认界面审核：            │
│  · 查看推荐的出库源垛位           │
│  · 若有倒库，审核倒库路径和临时存放 │
│  · 可切换备选垛位/调整方案        │
│  · 确认后方案生效                 │
└─────────────────────────────────┘
       │
       ↓（确认后）
大屏展示出库任务 / 语音播报指令
       │
       ↓
天车工吊运：货架 → 货车
调运工绑扎/拆解
       │
       ↓
机器狗扫描垛位确认：原数量 - 现数量 = 出库数量
       │
       ↓
库存更新 + 数字孪生同步
任务状态：已完成
```

### 4.2 出库调度算法

**输入**：
- 出库订单：`{合同号, 规格, 材质, 数量, 吨数, 长度范围}`
- 当前库存视图：所有垛位的钢材明细（含层位信息）
- 天车状态：各天车位置、忙闲状态

**输出**：
- 推荐出库源（可多方案排序）：`[{方案ID, 得分, 目标钢材定位: [{钢材ID, 垛位编号, 层位, 是否被压}], 方案说明}]`
- 推荐出库序列：`[{步骤, 操作类型(倒库/出库), 钢材ID, 源位置, 目标位置}]`

**算法逻辑**：

```
1. 检索同规格钢材的所有垛位
2. 对每个候选垛位，计算"取货代价"：
   代价 = 上方非目标钢材数量（需倒库数）+ 天车移动距离 + 垛位到货车距离
3. 按代价排序，推荐代价最小的垛位作为出库源（默认选中）
4. 若目标层位上方有压货，生成倒库子任务：
   倒库子任务 = {移走钢材ID, 临时存放垛位} → 插入出库任务序列之前
5. 生成天车执行序列：
   倒库步骤1 → 倒库步骤2 → ... → 出库吊运 → 复验确认

人工确认界面要素：
  · 推荐垛位及备选垛位列表（按代价排序）
  · 各方案的倒库步骤数、预计耗时对比
  · [确认推荐方案] [选择备选方案] [手动指定垛位] [否决]
```

### 4.3 出库状态机

| 状态 | 触发条件 | 系统动作 | 下一状态 |
|------|---------|---------|---------|
| **空闲** | — | 等待提货单 | — |
| **待盘库** | 提货单录入 | 推送订单至机器狗，机器狗前往目标货架 | — |
| **盘库中** | 机器狗就位 | 扫描确认目标钢材位置、层位、压货情况 | — |
| **方案生成** | 盘库完成 | 生成出库推荐方案（含倒库方案，如有压货），推送确认工作台 | — |
| **待确认** | 方案生成完毕 | 等待管理工审核确认（可调整/否决） | 出库执行/方案生成 |
| **出库执行** | 管理工确认方案 | 大屏展示+语音播报，天车吊运，逐根跟踪 | — |
| **完成** | 复验确认 | 库存落定，同步数字孪生 | 空闲 |

## 5. 倒库规划详细设计

### 5.1 倒库决策算法

**输入**：
- 目标钢材位置：`{垛位编号, 层位(从下往上数)}`
- 该垛位完整信息：`{每层的钢材ID、规格、是否属当前订单}`
- 可用临时存放位置：空垛位列表、同规格垛位（允许混放）

**输出**：
- 推荐倒库方案（可多方案排序）：`[{方案ID, 得分, 步骤列表: [{钢材ID, 源位置, 临时目标位置, 预计耗时}], 方案说明}]`
- 总代价评估：搬运次数、总移动距离

**算法逻辑**：

```
若目标钢材在第N层（从下往上），上方共M层：
  需要搬走的钢材 = M层中所有非本订单的钢材

方案生成（多方案比选）：
  对于每根需要搬走的钢材X：
    方案A：搬至最近的空垛位
    方案B：搬至同规格垛位（如果存在且有空间）
    方案C：搬至即将出库的垛位（如果已知后续订单）
    方案D：若后续需要X的规格，搬至货车直接出库（最优但依赖预判）

  生成1-3个可行方案，按总代价排序：
    代价 = Σ(搬运次数 × 单次搬运耗时) + Σ(天车移动距离)
    
  约束条件：
    · 搬运后不影响其他订单的出库
    · 临时存放不造成新的压货
    · 天车资源可用

人工确认界面要素：
  · 推荐方案及备选方案摘要（搬运步骤数、预计总耗时）
  · 倒库步骤图示（源垛位→临时存放→目标出库）
  · [确认推荐方案] [选择备选方案] [手动调整存放位置] [否决]
```

### 5.2 临时存放策略

| 策略 | 适用场景 | 优先级 |
|------|---------|--------|
| 搬至同规格垛位 | 存在同规格垛位且有空间 | 1（最优） |
| 搬至即将出库的垛位 | 已知后续订单需要该规格 | 2 |
| 搬至指定空垛位 | 预留的倒库缓冲区 | 3 |
| 搬至最近空垛位 | 无上述条件 | 4（兜底） |

### 5.3 倒库状态机

| 状态 | 触发条件 | 系统动作 | 下一状态 |
|------|---------|---------|---------|
| **空闲** | — | 等待倒库需求 | — |
| **压货识别** | 出库盘库发现压货 | 分析目标垛位层位结构，识别需搬离的钢材 | — |
| **方案生成** | 识别完成 | 生成倒库推荐方案（1-3个），推送确认工作台 | — |
| **待确认** | 方案生成完毕 | 等待管理工审核（可调整临时存放位置、步骤顺序） | 倒库执行/方案生成 |
| **倒库执行** | 管理工确认 | 按步骤执行：移走上层→临时存放→完成后触发原出库 | — |
| **完成** | 机器狗复验确认 | 涉及垛位全部更新，数字孪生同步 | 空闲 |

## 6. 人工确认工作台

### 6.1 定位

人工确认工作台是库管调度规划"半自动化"理念的核心载体——管理工在此审核所有系统推荐方案，行使最终决策权。

### 6.2 界面要素

```
┌──────────────────────────────────────────────────────────────┐
│  待确认任务  │  入库: 2  出库: 1  倒库: 0                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ 任务: IN-20260508-001  类型: 入库  优先级: P1 ────────┐  │
│  │                                                         │  │
│  │  货车: 鄂A12345  载货: Φ170×6根 + Φ150×4根             │  │
│  │                                                         │  │
│  │  ★ 推荐方案 (方案A, 得分 92/100):                        │  │
│  │    Φ170×6 → S-A-03-12 (同规格垛位, 已有4根, 剩余空间6根) │  │
│  │    Φ150×4 → S-A-05-08 (空垛位, 最近)                    │  │
│  │    天车分配: #3号天车                                    │  │
│  │                                                         │  │
│  │  ○ 备选方案B (得分 85/100)   ○ 备选方案C (得分 78/100)   │  │
│  │                                                         │  │
│  │  [✓ 一键确认]  [选择方案B]  [选择方案C]  [手动调整]  [否决] │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─ 任务: OUT-20260508-003  类型: 出库  优先级: P0 ───────┐  │
│  │  订单: ORD-2026-0501  规格: Φ170×10  数量: 4根          │  │
│  │                                                         │  │
│  │  ★ 推荐方案: 垛位 S-A-01-07 (代价最小, 无压货, 不需倒库) │  │
│  │    天车: #1号  预计耗时: 8分钟                           │  │
│  │                                                         │  │
│  │  ○ 备选方案B (同规格 S-A-03-12, 有2根压货需倒库, 耗时15分)│  │
│  │                                                         │  │
│  │  [✓ 一键确认]  [选择方案B]  [手动指定垛位]  [否决]       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  📋 确认日志: 今日已处理 5/6   │  [查看全部]                 │
└──────────────────────────────────────────────────────────────┘
```

### 6.3 操作权限

| 操作 | 说明 | 权限要求 |
|------|------|---------|
| 一键确认 | 采纳系统推荐方案，自动下发任务 | 管理工 |
| 选择备选方案 | 从备选方案中切换，确认后下发 | 管理工 |
| 手动调整 | 修改垛位分配/天车分配/步骤顺序 | 管理工 |
| 否决 | 退回系统，要求重新生成方案（附原因） | 管理工 |
| 超时自动提醒 | 待确认超过N分钟未处理，闪烁提醒 | 系统自动 |

### 6.4 确认日志

所有确认操作记录到日志，支持追溯：

| 字段 | 说明 |
|------|------|
| 任务ID | 关联的调度任务 |
| 操作类型 | 确认/调整/否决 |
| 操作人 | 管理工ID |
| 操作时间 | 精确到秒 |
| 推荐方案摘要 | 系统推荐的方案快照 |
| 最终方案 | 确认后的实际方案（若调整过则不同于推荐） |
| 调整说明 | 管理工填写的调整原因 |
| 否决原因 | 若否决，记录原因 |

## 7. 天车任务队列分配算法

### 7.1 问题建模

- 9个天车，最多同时动5个
- 3条产线，天车-产线存在映射关系
- 每个天车有独立的任务队列
- 任务来源：入库、出库、倒库
- **所有任务需管理工确认后才进入天车队列**

### 7.2 分配策略

```
当管理工确认方案后，系统自动将任务分发至天车队列：

1. 确定天车可执行的任务范围（基于产线映射、空间可达范围）
2. 从可执行任务中按优先级排序：
   P0：正在进行的出库任务（关联倒库的后续步骤）
   P1：出库任务（司机等待）
   P2：入库任务（货车等待）
   P3：倒库整理任务（非紧急）
3. 推荐优先级最高、距离最近的天车
4. 管理工可在确认界面调整天车分配
5. 确认后任务进入对应天车的执行队列
```

## 8. 数据融合引擎设计

### 8.1 数据源及字段

| 数据源 | 关键字段 | 获取方式 | 时效性 |
|--------|---------|---------|--------|
| 半成品库物流系统 | 车牌号、订单号、规格、数量、吨数、装车时间 | REST API | 装车完成时推送 |
| ERP/订单系统 | 合同号、客户、规格、材质、数量、吨数、交货期 | REST API | 按需查询 |
| 门卫扫码系统 | 提货单号、车牌号、到达时间 | REST API / MQ | 实时 |
| 机器狗扫描数据 | 钢材二维码、位置坐标、扫描时间、图片 | WebSocket/MQTT | 实时 |
| 库管系统 | 库存表、垛位表、出入库记录 | REST API | 读写双向 |

### 8.2 数据融合规则

```
数据冲突仲裁（优先级从高到低）：
  1. 机器狗现场扫描数据（最高优先级，以实物为准）
  2. 门卫扫码录入数据
  3. 半成品库物流系统数据
  4. ERP系统数据（最低优先级，可能滞后）

数据补全逻辑：
  · 若ERP有规格但无具体数量 → 以机器狗扫描为准
  · 若半成品库有订单号但无车牌号 → 以门卫扫码补全
  · 若所有系统数据缺失 → 标记为"待确认"，通知人工介入
```

## 9. 核心数据模型

### 9.1 垛位表 (stack_location)

| 字段 | 类型 | 说明 |
|------|------|------|
| stack_id | varchar(32) PK | 垛位唯一编号 |
| zone_id | varchar(32) FK | 所属库区 |
| row_no | int | 排号 |
| col_no | int | 列号 |
| center_x | float | 中心坐标X（现场坐标系） |
| center_y | float | 中心坐标Y |
| max_layers | int | 最大堆垛层数 |
| current_layers | int | 当前层数 |
| material_spec | varchar(64) | 当前存放规格（空则为空垛位） |
| total_count | int | 当前存放根数 |
| status | enum | 状态：empty/occupied/reserved/blocked |
| last_scan_time | datetime | 最后扫描时间 |
| last_scan_by | varchar(32) | 最后扫描机器狗ID |

### 9.2 钢材库存表 (steel_inventory)

| 字段 | 类型 | 说明 |
|------|------|------|
| steel_id | varchar(32) PK | 钢材唯一ID（二维码编码） |
| stack_id | varchar(32) FK | 所在垛位 |
| layer_no | int | 所在层位（从下往上，1开始） |
| position_in_layer | int | 层内序号（从左到右） |
| spec | varchar(64) | 规格型号（如 Φ170×10） |
| material | varchar(32) | 材质（如 45#钢） |
| length | float | 长度（米） |
| weight | float | 吨数 |
| order_id | varchar(64) | 关联订单号 |
| status | enum | 状态：in_stock/reserved/in_transit/out_stock |
| qr_code_data | text | 二维码原始数据 |
| create_time | datetime | 入库时间 |
| update_time | datetime | 最后更新时间 |

### 9.3 调度任务表 (dispatch_task)

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | varchar(32) PK | 任务唯一ID |
| task_type | enum | 类型：inbound/outbound/relocation/inspection |
| order_id | varchar(64) | 关联订单号 |
| plate_no | varchar(32) | 关联车牌号 |
| status | enum | 状态：draft/pending_approval/approved/rejected/dispatched/in_progress/completed/cancelled |
| priority | enum | 优先级：P0/P1/P2/P3 |
| recommended_plan | json | 系统推荐方案（含备选方案列表） |
| approved_plan | json | 管理工确认的最终方案（可不同于推荐） |
| approved_by | varchar(32) | 确认人ID |
| approved_time | datetime | 确认时间 |
| adjust_comment | varchar(256) | 管理工调整说明 |
| created_time | datetime | 创建时间 |
| dispatched_time | datetime | 下发时间 |
| completed_time | datetime | 完成时间 |
| assigned_crane | varchar(16) | 分配的天车编号 |

### 9.4 任务步骤表 (task_step)

| 字段 | 类型 | 说明 |
|------|------|------|
| step_id | varchar(32) PK | 步骤ID |
| task_id | varchar(32) FK | 所属任务 |
| seq_no | int | 步骤序号 |
| operation | enum | 操作：scan/lift/move/place/verify |
| steel_id | varchar(32) | 目标钢材ID |
| source_location | varchar(64) | 源位置描述 |
| target_location | varchar(64) | 目标位置描述 |
| status | enum | 状态：pending/in_progress/completed |
| crane_id | varchar(16) | 执行天车 |
| started_time | datetime | 开始时间 |
| completed_time | datetime | 完成时间 |

## 10. 接口设计

### 10.1 对外部系统的接口（库管调度规划作为消费方）

| 接口 | 提供方 | 方法 | 说明 |
|------|--------|------|------|
| `/api/wip/shipment/notify` | 半成品库系统 | POST | 装车完成推送车牌+订单明细 |
| `/api/erp/order/{order_id}` | ERP系统 | GET | 按订单号查询详情 |
| `/api/gate/scan/arrival` | 门卫系统 | POST | 提货单扫码录入 |
| `/api/warehouse/inventory` | 库管系统 | GET/PUT | 库存查询/更新 |

### 10.2 对内部的接口（库管调度规划作为提供方）

| 接口 | 消费方 | 方法 | 说明 |
|------|--------|------|------|
| `/api/plan/recommend/{task_id}` | 管理工确认工作台 | GET | 获取推荐方案（含备选） |
| `/api/plan/approve/{task_id}` | 管理工确认工作台 | POST | 确认/调整/否决方案 |
| `/api/plan/task/create` | 任务规划系统 | POST | 确认后生成机器狗任务 |
| `/api/plan/task/{id}/status` | 任务规划系统 | PUT | 更新任务状态 |
| `/api/plan/crane/assign` | 大屏/小程序 | GET | 获取天车分配结果 |
| `/api/plan/digital-twin/sync` | 数字孪生 | WebSocket | 推送库存变更事件 |
| `/api/plan/announce` | 消息分发 | POST | 触发大屏/语音/小程序通知 |
| `/api/plan/approval/log` | 审计系统 | GET | 查询确认操作日志 |

---

# 第二部分：机器狗任务规划管理系统

## 1. 系统定位

机器狗任务规划管理系统是连接"库管调度规划的推荐方案"与"机器狗物理执行"的中间层，负责：

- 接收库管调度规划经管理工确认后下发的业务任务（盘库、扫描、复验）
- 将业务任务拆解为机器狗可执行的原子动作序列
- 管理机器狗巡检计划（周期性、事件驱动、手动触发）
- 实时监控任务执行状态，处理异常
- 负责任务闭环确认（扫描比对→结果判定→数据同步）

## 2. 功能模块架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    机器狗任务规划管理系统                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 任务接收模块  │  │ 任务拆解模块  │  │ 路径规划模块  │              │
│  │             │  │             │  │             │              │
│  │ ·调度任务接收│  │ ·原子动作生成│  │ ·最优路径计算│              │
│  │ ·巡检计划触发│  │ ·依赖关系编排│  │ ·避障路径重算│              │
│  │ ·手动任务创建│  │ ·执行序列优化│  │ ·回充路径规划│              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────┐                │
│  │              任务调度引擎                     │                │
│  │                                             │                │
│  │  ·多机器狗任务负载均衡    ·优先级抢占调度      │                │
│  │  ·任务队列管理           ·资源冲突检测        │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │              任务执行监控                     │                │
│  │                                             │                │
│  │  ·实时状态跟踪    ·执行进度计算               │                │
│  │  ·异常检测告警    ·超时处理                   │                │
│  │  ·任务闭环判定    ·执行日志记录               │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │              机器狗通信层                     │                │
│  │                                             │                │
│  │  ·WebSocket实时通信   ·指令下发与ACK         │                │
│  │  ·数据回传处理        ·心跳与断连重连        │                │
│  │  ·离线任务队列        ·数据本地缓存          │                │
│  └─────────────────────────────────────────────┘                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 任务类型定义

### 3.1 任务类型枚举

| 任务类型 | 触发方式 | 优先级 | 说明 |
|---------|---------|--------|------|
| **入库扫描** | 事件驱动（货车到达） | P1 | 机器狗前往货车尾部，扫描断面/内壁识别钢材 |
| **入库复验** | 事件驱动（吊运完成） | P1 | 机器狗扫描目标垛位，确认入库数量 |
| **出库盘库** | 事件驱动（提货单录入） | P0 | 机器狗提前到达目标货架，确认库存和压货情况 |
| **出库复验** | 事件驱动（吊运完成） | P0 | 机器狗扫描垛位，确认出库数量 |
| **倒库扫描** | 事件驱动（倒库触发） | P1 | 扫描涉及倒库的垛位，确认移走/归位数量 |
| **定时巡检** | 周期触发 | P3 | 按预设路线逐垛位扫描，更新库存 |
| **指定点巡检** | 手动触发 | P3 | 管理人员指定垛位，机器狗前往扫描 |
| **充电任务** | 电量触发 | P2 | 电量低于阈值，自动返回充电站 |
| **紧急召回** | 手动触发 | P0 | 管理员远程召回机器狗 |

### 3.2 任务优先级抢占规则

```
P0（紧急）：出库盘库、出库复验、紧急召回
  → 可抢占当前执行中的P1/P2/P3任务
  → P0间按FIFO执行

P1（重要）：入库扫描、入库复验、倒库扫描
  → 不可抢占P0，可抢占P2/P3
  → P1间按FIFO执行

P2（一般）：充电任务
  → 不可抢占P0/P1，可抢占P3
  → 电量<20%时自动升级为P1

P3（常规）：定时巡检、指定点巡检
  → 不可抢占任何更高优先级
  → 巡检中若有P0/P1插入，记录已巡检位置，恢复后继续
```

## 4. 任务拆解设计

### 4.1 原子动作定义

机器狗的每个任务被拆解为一系列原子动作：

| 原子动作 | 指令 | 参数 | 预计耗时 |
|---------|------|------|---------|
| **Navigate** | 导航至目标点 | 目标坐标(x,y,z)、避障模式 | 按距离计算 |
| **Stand** | 调整站立姿态 | 高度、俯仰角 | 3-5秒 |
| **Scan** | 执行扫描 | 扫描模式(断面/内壁/端面)、相机参数 | 10-30秒 |
| **Recognize** | 识别二维码/OCR | 识别模式、ROI区域 | 3-5秒 |
| **Capture** | 拍摄照片 | 相机编号、分辨率 | 1-2秒 |
| **Wait** | 原地等待 | 等待时长或等待信号 | 可变 |
| **Charge** | 自主充电对接 | 充电站ID | 30-60秒 |
| **Announce** | 语音播报 | 播报文本 | 按文本长度 |

### 4.2 任务拆解示例

**任务：入库扫描**

```
Task: 入库扫描（货车 鄂A12345）
├── Step 1: Navigate → 货车尾部停靠点(坐标P1)
├── Step 2: Stand → 扫描姿态(高度H_scan, 俯仰角0°)
├── Step 3: Scan → 断面模式(逐层扫描, 4-6层)
├── Step 4: Recognize → 每层每根钢管的端面二维码
├── Step 5: Capture → 全景照片(货车+堆垛)
├── Step 6: [数据回传] 识别结果 + 照片 → 数据融合引擎
└── Step 7: [等待] 入库调度方案生成
```

**任务：出库盘库**

```
Task: 出库盘库（垛位 S-A-03-12）
├── Step 1: Navigate → 垛位S-A-03-12前方停靠点(坐标P2)
├── Step 2: Stand → 盘库姿态
├── Step 3: Scan → 端面模式(扫描垛位所有层)
├── Step 4: Recognize → 逐层识别钢材二维码
├── Step 5: [数据回传] 垛位实际库存 vs 系统库存比对
├── Step 6: [等待] 调度系统判断是否倒库
└── Step 7: Announce → 播报盘库结果("S-A-03-12, Φ170×8根, 确认完成")
```

### 4.3 任务模板化

系统预置任务模板，根据业务场景自动填充参数：

```json
{
  "template_id": "TPL_INBOUND_SCAN",
  "template_name": "入库扫描",
  "steps": [
    {"action": "Navigate", "param": "{truck_tail_position}"},
    {"action": "Stand", "param": {"height": "{scan_height}", "pitch": 0}},
    {"action": "Scan", "param": {"mode": "cross_section", "layers": "{stack_layers}"}},
    {"action": "Recognize", "param": {"mode": "qr_code", "roi": "full"}},
    {"action": "Capture", "param": {"camera": "main", "resolution": "2688x1520"}}
  ]
}
```

## 5. 路径规划设计

### 5.1 路径规划约束

| 约束项 | 说明 |
|--------|------|
| 固定路径优先 | 日常巡检沿业主规划的巡检道路执行 |
| 障碍物检测 | 激光雷达实时检测，遇障碍绕行或停障 |
| 最短路径 | 事件驱动任务（入库/出库）计算当前位置到目标的最短可行路径 |
| 电量约束 | 路径规划需考虑剩余电量，确保能返回充电站 |
| 避让天车 | 路径避免穿越天车作业区下方（如有天车状态数据） |

### 5.2 路径计算

```
路径规划输入：
  · 起点：当前坐标(x1, y1, z1)
  · 终点：目标坐标(x2, y2, z2)
  · 地图：预构建的3D导航地图（含可行走区域、障碍物、充电站位置）

路径规划输出：
  · 路径点序列：[{x, y, z}, ...]
  · 预计距离、预计耗时
  · 途经充电站（如需要中途补电）

算法：
  1. 在预构建导航地图上运行A*算法
  2. 代价函数：f(n) = g(n) + h(n)
     g(n)：已走距离
     h(n)：到目标点的欧几里得距离
  3. 动态避障：行驶过程中，若激光雷达检测到新障碍物
     → 原地重算局部路径（局部D* Lite）
     → 若无法绕行，停障并告警
```

## 6. 任务闭环判定

### 6.1 闭环判定流程

```
任务闭环 = 执行前状态 + 执行动作 + 执行后状态 → 对比验证

以出库为例：
  1. 执行前：机器狗扫描垛位 → 10根钢管，上传快照S0
  2. 执行中：天车吊走4根
  3. 执行后：机器狗再次扫描 → 6根钢管，上传快照S1
  4. 系统比对：
     S0.count - S1.count = 4
     order.qty = 4
     4 == 4 → ✅ 任务完成
  5. 更新库存：垛位减少4根，4根标记"已出库"
  6. 数字孪生同步

异常处理：
  · 若 S0.count - S1.count != order.qty → ⚠️ 数量不匹配，人工复核
  · 若 S0的钢材ID与S1的不一致 → ⚠️ 可能吊错了，暂停并告警
  · 若 S1扫描失败 → ⚠️ 机器狗调整位置重试，重试3次后人工介入
```

### 6.2 闭环状态机

```
┌──────────┐               ┌──────────┐
│ 任务创建  │ ───────────→  │ 待执行   │
└──────────┘               └─────┬────┘
                                 │
                    任务下发至机器狗
                                 ↓
                           ┌──────────┐
                           │ 执行中   │
                           └─────┬────┘
                                 │
                    最后一个原子动作完成
                                 ↓
                           ┌──────────┐
                           │ 待验证   │ ←── 机器狗上传最终扫描数据
                           └─────┬────┘
                                 │
                    数据比对结果
                    ┌────────────┼────────────┐
                    ↓            ↓            ↓
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │ 已完成   │ │ 需重试   │ │ 异常     │
              └──────────┘ └──────────┘ └──────────┘
                                  │            │
                             重试≤3次    人工介入
                                  │
                                  └──→ 待执行
```

## 7. 核心数据模型

### 7.1 机器狗任务表 (robot_task)

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | varchar(32) PK | 任务ID |
| robot_id | varchar(32) FK | 机器狗ID |
| task_type | enum | 见3.1任务类型枚举 |
| priority | enum | P0/P1/P2/P3 |
| status | enum | created/dispatched/in_progress/pending_verify/completed/failed/cancelled |
| trigger_source | enum | event/schedule/manual |
| ref_biz_task_id | varchar(32) | 关联库管调度规划任务ID |
| target_location_id | varchar(64) | 目标位置（垛位/货车编号等） |
| target_coord_x | float | 目标坐标X |
| target_coord_y | float | 目标坐标Y |
| planned_path | json | 预规划路径点序列 |
| created_time | datetime | 创建时间 |
| dispatched_time | datetime | 下发时间 |
| started_time | datetime | 开始时间 |
| completed_time | datetime | 完成时间 |
| retry_count | int | 重试次数 |

### 7.2 任务步骤表 (robot_task_step)

| 字段 | 类型 | 说明 |
|------|------|------|
| step_id | varchar(32) PK | 步骤ID |
| task_id | varchar(32) FK | 所属任务 |
| seq_no | int | 步骤序号 |
| action | enum | 原子动作类型（Navigate/Stand/Scan/Recognize/...） |
| params | json | 动作参数 |
| status | enum | pending/in_progress/completed/failed/skipped |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| result_data | json | 动作结果数据（如扫描识别的钢材列表） |
| error_msg | text | 错误信息 |

### 7.3 任务验证表 (task_verification)

| 字段 | 类型 | 说明 |
|------|------|------|
| verification_id | varchar(32) PK | 验证ID |
| task_id | varchar(32) FK | 关联任务 |
| snapshot_before | json | 执行前快照（{stack_id, count, steel_ids[]}） |
| snapshot_after | json | 执行后快照 |
| expected_change | json | 预期变化（{type: add/remove, qty: N}） |
| actual_change | json | 实际变化 |
| match_result | enum | matched/mismatched/partial/error |
| created_time | datetime | 验证时间 |

### 7.4 巡检计划表 (patrol_plan)

| 字段 | 类型 | 说明 |
|------|------|------|
| plan_id | varchar(32) PK | 计划ID |
| plan_name | varchar(64) | 计划名称 |
| plan_type | enum | daily/weekly/custom |
| robot_id | varchar(32) | 指定机器狗（可为空，自动分配） |
| route_points | json | 巡检路径点序列 |
| cron_expression | varchar(32) | 触发周期（cron表达式） |
| enabled | bool | 是否启用 |
| last_exec_time | datetime | 上次执行时间 |
| next_exec_time | datetime | 下次执行时间 |

## 8. 接口设计

### 8.1 对库管调度规划的接口（消费者）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/robot/task/dispatch` | POST | 库管调度规划经人工确认后下发业务任务 |
| `/api/robot/task/{id}/status` | GET | 库管调度规划查询任务状态 |
| `/api/robot/task/{id}/cancel` | POST | 库管调度规划取消任务（需管理工权限） |

### 8.2 对机器狗本体的接口（双向）

| 通信方式 | 方向 | 说明 |
|---------|------|------|
| WebSocket | 下发 | 任务指令下发（含完整步骤序列） |
| WebSocket | 下发 | 路径规划结果推送 |
| WebSocket | 上传 | 实时状态上报（位置、电量、当前步骤） |
| WebSocket | 上传 | 扫描/识别结果回传 |
| WebSocket | 上传 | 告警/异常上报 |
| MQTT | 上传 | 心跳（1Hz） |
| MQTT | 上传 | 视频流地址（RTSP URL） |
| HTTP | 上传 | 大文件上传（照片、日志） |

### 8.3 任务下发协议

```json
{
  "msg_type": "task_dispatch",
  "task_id": "T-20260508-001",
  "priority": "P1",
  "steps": [
    {
      "seq": 1,
      "action": "Navigate",
      "params": {
        "target": {"x": 123.45, "y": 67.89, "z": 0.0},
        "mode": "optimal",
        "max_speed": 1.2
      }
    },
    {
      "seq": 2,
      "action": "Scan",
      "params": {
        "mode": "cross_section",
        "camera": "main",
        "layers": {"min": 1, "max": 6}
      }
    }
  ],
  "timeout": 300,
  "retry_policy": {"max_retries": 3, "backoff": 30}
}
```

---

# 第三部分：数字孪生系统

## 1. 系统定位

数字孪生系统的定位是**超前调度决策平台**，而非事后可视化工具。其核心价值：

- **实时映射**：仓库物理状态（垛位、钢材、机器狗、天车）的秒级数字镜像
- **超前预演**：在货车到达前，预生成调度方案的三维可视化展示
- **决策支撑**：通过颜色编码、高亮、虚线等方式直观呈现库存状态和任务进度
- **历史追溯**：任意时刻的库存快照和任务轨迹回放

## 2. 功能模块架构

```
┌─────────────────────────────────────────────────────────────────┐
│                       数字孪生系统                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 3D场景渲染   │  │ 实时数据驱动  │  │ 预测展示模块  │              │
│  │             │  │             │  │             │              │
│  │ ·厂房建筑   │  │ ·垛位状态   │  │ ·来车预演   │              │
│  │ ·垛位区域   │  │ ·机器狗位置 │  │ ·调度方案   │              │
│  │ ·机器狗模型 │  │ ·钢材库存   │  │ ·虚线高亮   │              │
│  │ ·钢材示意体 │  │ ·任务进度   │  │ ·路径预测   │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────┐                │
│  │              数据接入层                       │                │
│  │                                             │                │
│  │  ·WebSocket事件订阅  ·REST API查询           │                │
│  │  ·数据缓冲队列       ·状态快照缓存           │                │
│  └─────────────────────┬───────────────────────┘                │
│                        ↓                                         │
│  ┌─────────────────────────────────────────────┐                │
│  │              交互与视图层                     │                │
│  │                                             │                │
│  │  ·数字孪生大屏     ·管理后台PC端              │                │
│  │  ·自由视角/俯瞰/第一人称    ·点击查询详情     │                │
│  │  ·历史回放控制条   ·图层开关控制              │                │
│  └─────────────────────────────────────────────┘                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 建模分层策略

### 3.1 精度分级

| 层级 | 建模对象 | 精度要求 | 面片预算 | 更新频率 |
|------|---------|---------|---------|---------|
| L0 - 精确建模 | 厂房建筑 | 构件≤10cm误差 | ≤500万面（整体） | 一次性 |
| L0 - 精确建模 | 垛位区域 | 中心坐标≤5cm误差 | 每个垛位≤2000面 | 一次性 |
| L1 - 精细模型 | 机器狗 | 外观一致、状态可视化 | 每台≤5万面 | 实时驱动 |
| L1 - 精细模型 | 天车（可选） | 含吊钩、大车、小车 | 每台≤3万面 | 如接数据则实时 |
| L2 - 示意体 | 钢材 | 块状示意，高度随数量变化 | 每个垛位≤500面 | 库存变化时 |
| L3 - 简化模型 | 充电桩、摄像头等 | 位置准确即可 | 每个≤1000面 | 一次性 |

### 3.2 钢材示意体设计

```
垛位钢材示意体 = 简化的长方体块

高度计算：h = current_layers × layer_height_unit
颜色材质：根据垛位状态动态切换
  · 🟢 绿色半透明：已扫描，数据正常
  · ⚪ 灰色：未扫描
  · 🔵 蓝色脉冲动画：正在扫描中
  · 🟡 黄色闪烁：数据异常，需复核
  · 🟠 橙色半透明：空垛位

点击交互：
  单击垛位 → 弹出信息面板：
    · 垛位编号：S-A-03-12
    · 主要规格：Φ170×10
    · 钢材根数：8根
    · 总吨数：24.5吨
    · 所属订单：ORD-2026-0501
    · 最后扫描：2026-05-08 14:32:15
    · 扫描机器狗：DG-001
```

### 3.3 机器狗模型

| 视觉元素 | 设计 | 驱动方式 |
|---------|------|---------|
| 3D模型 | 四足机器人精细模型，含机身、腿、摄像头云台 | 静态资产 |
| 位置 | (x, y, z) 实时同步 | 机器狗上报坐标驱动 |
| 朝向 | yaw角 | 机器狗上报朝向驱动 |
| 云台角度 | pitch角 | 机器狗上报云台角度 |
| 状态灯 | 顶部呼吸灯 | 见下表 |
| 运动动画 | 行走/站立/趴下/充电 | 根据任务状态切换 |

**状态灯定义**：

| 颜色 | 动画 | 含义 |
|------|------|------|
| 🟢 绿色 | 呼吸灯（2s周期） | 巡检中 |
| 🔵 蓝色 | 常亮 | 前往任务目标 |
| ⚪ 白色 | 无动画 | 待命中 |
| 🟡 黄色 | 闪烁（1s周期） | 充电中 |
| 🔴 红色 | 快闪（0.5s周期） | 故障/告警 |
| 🔵 蓝色 | 脉冲 | 正在扫描识别 |

## 4. 实时数据驱动设计

### 4.1 数据订阅通道

```
数字孪生系统订阅以下事件流：

通道1：库存变更事件 (来自库管调度规划)
  Event: INVENTORY_CHANGED
  Payload: {
    stack_id, change_type (add/remove/move),
    steel_list: [{steel_id, spec, ...}],
    before_snapshot, after_snapshot,
    timestamp
  }

通道2：任务状态事件 (来自库管调度规划)
  Event: TASK_STATUS_CHANGED
  Payload: {
    task_id, task_type, old_status, new_status,
    related_stacks[], assigned_crane,
    timestamp
  }

通道3：机器狗实时位姿 (来自任务规划系统)
  Event: ROBOT_POSE_UPDATE (1Hz)
  Payload: {
    robot_id, position{x,y,z}, orientation{yaw,pitch,roll},
    speed, battery, current_task_id,
    timestamp
  }

通道4：机器狗扫描事件 (来自任务规划系统)
  Event: ROBOT_SCAN_COMPLETE
  Payload: {
    robot_id, task_id, scan_target_type, scan_target_id,
    identified_steels[], scan_quality, timestamp
  }
```

### 4.2 延迟要求

| 数据 | 最大延迟 | 实现方式 |
|------|---------|---------|
| 机器狗位置更新 | ≤1秒 | WebSocket + 前端插值平滑 |
| 垛位状态变更 | ≤5秒 | WebSocket事件推送 |
| 任务状态变更 | ≤2秒 | WebSocket事件推送 |
| 库存数据刷新 | ≤5秒 | 事件驱动 + 定时轮询兜底 |
| 视频流 | ≤500ms | RTSP → WebRTC转码 |

## 5. 预测调度展示

### 5.1 来车预演

当半成品库推送来车信息后（提前30分钟）：

```
预演流程：
1. 数字孪生场景边缘出现货车图标（半透明），标注"预计到达：14:30"
2. 目标垛位虚线高亮 → 显示"预分配"
3. 从天车当前位置到目标垛位之间，显示虚线路径
4. 鼠标悬停货车图标 → 弹出预计载货清单
5. 货车到达，机器狗扫描确认后 → 半透明转为实体，虚线转为实线
```

### 5.2 调度方案预览

```
出库场景预览：
1. 提货单录入 → 目标垛位高亮（黄色边框闪烁）
2. 盘库完成 → 目标钢材层位高亮（红色，被压货时为红色+橙色分层）
3. 若有倒库 → 倒库路径虚线动画（移走上层→临时垛位→目标出库）
4. 出库执行 → 吊运动画（钢材块从垛位移动到货车）
5. 任务完成 → 高亮消失，垛位更新
```

### 5.3 颜色编码规范

| 场景 | 颜色 | 含义 |
|------|------|------|
| 垛位状态 | 🟢绿/⚪灰/🔵蓝/🟡黄/🟠橙 | 见3.2 |
| 任务目标垛位 | 🟡黄色边框闪烁 | 当前任务目标 |
| 倒库源垛位 | 🔴红色边框 | 需移走的压货源 |
| 倒库目标垛位 | 🟠橙色虚线边框 | 临时存放位置 |
| 天车路径 | 🔵蓝色虚线 | 天车吊运路径 |
| 机器狗路径 | 🟢绿色虚线 | 机器狗导航路径 |
| 预分配（未到达） | ⚪白色半透明+虚线 | 待确认 |
| 已确认（到达后） | 🟢绿色实线边框 | 已确认 |

## 6. 历史回放

### 6.1 回放能力

| 功能 | 说明 |
|------|------|
| 时间轴拖动 | 支持拖动到任意历史时刻，场景状态还原至该时刻 |
| 快照粒度 | 关键事件时刻（任务开始/完成）+ 定时快照（每5分钟） |
| 回放速度 | 1x / 2x / 4x / 8x / 16x |
| 任务轨迹 | 显示机器狗和天车在回放时段的运动轨迹 |
| 库存对比 | 选择两个时刻，对比库存差异（高亮变化） |

### 6.2 快照数据结构

```json
{
  "snapshot_id": "SS-20260508-143000",
  "timestamp": "2026-05-08T14:30:00+08:00",
  "stacks": [
    {
      "stack_id": "S-A-03-12",
      "steel_count": 10,
      "steel_specs": {"Φ170×10": 6, "Φ150×8": 4},
      "status": "occupied"
    }
  ],
  "robots": [
    {
      "robot_id": "DG-001",
      "position": {"x": 123.4, "y": 67.8, "z": 0},
      "battery": 85,
      "current_task": "T-20260508-001"
    }
  ],
  "active_tasks": ["T-20260508-001", "T-20260508-002"]
}
```

## 7. 渲染性能设计

### 7.1 性能指标

| 指标 | 目标值 | 测试条件 |
|------|--------|---------|
| 渲染帧率 | ≥50 FPS | RTX 3060及以上 |
| 首屏加载 | ≤8秒 | 千兆网络 |
| 内存占用 | ≤2GB | Chrome/Edge |
| 场景面片总量 | ≤500万面 | 含全部LOD |
| 同时在线机器狗 | ≥10台 | 实时驱动 |
| 浏览器兼容 | Chrome/Edge 90+ | Windows 10+ |

### 7.2 优化策略

| 策略 | 说明 |
|------|------|
| 静态烘焙 | 厂房建筑、垛位区域预烘焙AO和光照贴图 |
| 实例化渲染 (GPU Instancing) | 钢材示意体使用同一Mesh+Material，通过实例化渲染 |
| 视锥体裁剪 | 仅渲染相机视野内的对象 |
| 遮挡剔除 | 被厂房结构遮挡的对象不渲染 |
| LOD分级 | 远距离模型降低面数（机器狗3级LOD） |
| 按需加载 | 仅加载当前库区的垛位数据，其他库区按需加载 |
| 纹理压缩 | 使用压缩纹理格式（Basis/KTX2） |
| Web Worker | 数据解析、格式转换在Worker线程执行 |

### 7.3 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 3D渲染引擎 | Unity → WebGL | 或 Three.js（轻量场景可考虑） |
| 前端框架 | React/Vue | 管理UI面板、控制栏等2D界面 |
| 数据通信 | WebSocket (SignalR / Socket.IO) | 实时事件推送 |
| 视频流 | WebRTC | RTSP → WebRTC转码，低延迟视频 |
| 后端API | .NET / Node.js | REST API提供快照查询、历史数据 |
| 数据库 | PostgreSQL + TimescaleDB | 时序快照存储 |
| 缓存 | Redis | 实时状态热数据 |

## 8. 核心数据模型

### 8.1 数字孪生场景配置表 (dt_scene_config)

| 字段 | 类型 | 说明 |
|------|------|------|
| scene_id | varchar(32) PK | 场景ID（对应库区） |
| scene_name | varchar(64) | 场景名称 |
| asset_bundle_url | varchar(256) | 3D资产包URL |
| camera_default | json | 默认相机位置{pos, target, fov} |
| camera_presets | json | 预设视角列表 |
| created_time | datetime | 创建时间 |

### 8.2 状态快照表 (dt_state_snapshot)

| 字段 | 类型 | 说明 |
|------|------|------|
| snapshot_id | varchar(32) PK | 快照ID |
| timestamp | datetime | 快照时间 |
| scene_id | varchar(32) FK | 所属场景 |
| trigger_type | enum | 触发类型：event/timer/manual |
| ref_event_id | varchar(64) | 关联事件ID（如任务完成事件） |
| snapshot_data | jsonb | 完整快照数据 |
| retention_days | int | 保留天数（热数据7天，冷数据90天归档） |

### 8.3 历史回放索引表 (dt_replay_index)

| 字段 | 类型 | 说明 |
|------|------|------|
| index_id | varchar(32) PK | 索引ID |
| start_time | datetime | 回放起始时间 |
| end_time | datetime | 回放结束时间 |
| snapshot_count | int | 包含快照数 |
| event_count | int | 包含事件数 |
| robot_ids | json | 涉及的机器狗ID列表 |
| task_ids | json | 涉及的任务ID列表 |
| total_duration_sec | int | 总时长（秒） |

## 9. 界面布局设计

### 9.1 数字孪生大屏（仓库现场LED屏）

```
┌──────────────────────────────────────────────────────────┐
│                 大冶特钢钢材库智能调度系统                    │
├────────────────────────────┬─────────────────────────────┤
│                            │   📋 当前任务                  │
│                            │   任务: 出库 Φ170×4根         │
│      3D 数字孪生场景        │   目标: S-A-03-12             │
│      (主视图, 占70%)       │   天车: #3号 调运工: 张三      │
│                            │   状态: 倒库中 [■■■■□□] 60%   │
│                            ├─────────────────────────────┤
│                            │   📊 今日概览                  │
│                            │   入库: 12车  出库: 8车        │
│                            │   倒库: 3次   巡检: 覆盖92%    │
│                            ├─────────────────────────────┤
│                            │   ⚠️ 待处理                    │
│                            │   无异常                       │
└────────────────────────────┴─────────────────────────────┘
```

### 9.2 管理后台PC端

```
┌──────────────────────────────────────────────────────────┐
│  导航: [数字孪生] [任务管理] [库存管理] [历史回放] [报表]    │
├──────────────────────────────────────────────────────────┤
│                                                          │
│   ┌─────────────────────────────────┐  ┌──────────────┐  │
│   │                                 │  │ 图层控制     │  │
│   │       3D 数字孪生视图            │  │ ☑ 机器狗     │  │
│   │       (可自由旋转/缩放)          │  │ ☑ 垛位状态   │  │
│   │                                 │  │ ☑ 天车       │  │
│   │                                 │  │ ☑ 巡检路径   │  │
│   │                                 │  │ ☐ 钢材标签   │  │
│   │                                 │  │ ☐ 视频流     │  │
│   └─────────────────────────────────┘  └──────────────┘  │
│                                                          │
│   ┌─────────────────────────────────────────────────────┐ │
│   │ 时间轴 (历史回放模式)                                 │ │
│   │ ◀◀  ▶  ▶▶  ●━━━━━━━━━━━━━━━━━━━━━━━━━━○            │ │
│   │ 08:00    10:00    12:00    14:00    16:00    18:00   │ │
│   └─────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 10. 接口设计

### 10.1 对外提供的查询接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/dt/scene/{id}/state` | GET | 获取当前场景完整状态 |
| `/api/dt/stack/{id}/detail` | GET | 获取单个垛位详情 |
| `/api/dt/replay?from=&to=` | GET | 获取指定时间段回放数据 |
| `/api/dt/snapshot/{timestamp}` | GET | 获取指定时刻快照 |
| `/api/dt/coverage/stats` | GET | 获取巡检覆盖率统计 |
| `/api/dt/coverage/trend?days=7` | GET | 获取覆盖率趋势 |

### 10.2 订阅的事件接口（消费者）

| 事件 | 来源 | 说明 |
|------|------|------|
| `INVENTORY_CHANGED` | 库管调度规划 | 库存变更 → 更新垛位颜色/数量 |
| `TASK_STATUS_CHANGED` | 库管调度规划 | 任务状态变化 → 更新任务面板 |
| `ROBOT_POSE_UPDATE` | 任务规划系统 | 机器狗位姿 → 驱动3D模型 |
| `ROBOT_SCAN_COMPLETE` | 任务规划系统 | 扫描完成 → 更新垛位扫描状态 |
| `CRANE_STATUS_UPDATE` | 天车系统（预留） | 天车状态 → 驱动天车模型 |

---

# 附录：三系统间接口矩阵

## 数据流向总图

```
                    ┌──────────────┐
                    │   外部系统    │
                    │ 半成品库/ERP  │
                    │ 门卫/库管    │
                    └──────┬───────┘
                           │ REST API
                           ↓
              ┌────────────────────────┐
              │     库管调度规划         │
              │  (第一部分)              │
              │                        │
              │  ·入库/出库/倒库规划    │
              │  ·方案推荐+人工确认     │
              │  ·库存状态管理          │
              │  ·数据融合引擎          │
              └────┬──────────┬────────┘
                   │          │
        ┌──────────┘          └──────────┐
        │ 确认后下发任务      推送库存/任务事件│
        ↓                                ↓
  ┌──────────────────┐         ┌──────────────────┐
  │ 机器狗任务规划管理 │         │   数字孪生系统    │
  │  (第二部分)        │         │   (第三部分)      │
  │                   │         │                  │
  │ ·任务拆解与分配   │ 位姿/扫描│ ·3D场景渲染      │
  │ ·路径规划         │ ──────→ │ ·实时状态展示     │
  │ ·执行监控与闭环   │  结果回传│ ·预测调度可视化    │
  └────────┬─────────┘         └──────────────────┘
           │
           │ WebSocket/MQTT
           ↓
    ┌──────────────┐
    │  机器狗本体   │
    └──────────────┘
```

## 接口清单

| 序号 | 提供方 | 消费方 | 接口 | 协议 | 说明 |
|------|--------|--------|------|------|------|
| 1 | 库管调度规划 | 任务规划 | 业务任务下发 | REST API | 确认后的入库/出库/倒库任务 |
| 2 | 库管调度规划 | 任务规划 | 任务取消 | REST API | 取消已下发任务（需管理工权限） |
| 3 | 库管调度规划 | 数字孪生 | 库存变更事件 | WebSocket | 垛位状态/数量变更推送 |
| 4 | 库管调度规划 | 数字孪生 | 任务状态事件 | WebSocket | 任务生命周期状态推送 |
| 5 | 库管调度规划 | 数字孪生 | 库存查询 | REST API | 历史快照/报表数据 |
| 6 | 任务规划 | 库管调度规划 | 任务状态回执 | REST API | 机器狗任务执行状态 |
| 7 | 任务规划 | 库管调度规划 | 扫描结果上报 | REST API | 识别数据回传 |
| 8 | 任务规划 | 数字孪生 | 机器狗位姿 | WebSocket | 1Hz实时位姿推送 |
| 9 | 任务规划 | 数字孪生 | 扫描完成事件 | WebSocket | 扫描结果通知 |
| 10 | 任务规划 | 机器狗 | 任务指令 | WebSocket | 任务+路径下发 |
| 11 | 任务规划 | 机器狗 | 控制指令 | WebSocket | 暂停/恢复/召回 |
| 12 | 机器狗 | 任务规划 | 状态上报 | WebSocket | 位姿/电量/当前步骤 |
| 13 | 机器狗 | 任务规划 | 扫描数据 | WebSocket | 识别结果实时回传 |
| 14 | 机器狗 | 任务规划 | 告警 | WebSocket | 异常/故障上报 |
| 15 | 机器狗 | 任务规划 | 心跳 | MQTT | 1Hz保活 |

---

> **文档状态**：初稿，待团队评审。
> **关联文档**：
> - 原始设计方案：`大冶特殊钢有限公司物流部钢材库巡检机器狗项目设计方案v1.1`
> - 会议纪要：`会议核心要点整理-2026-05-06.md`
> - 差距分析：`方案差距分析-2026-05-06.md`
> - 整合方案：`智能调度系统方案-大冶特钢-整合版.md`
